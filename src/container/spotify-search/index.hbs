<main class="main">
    {{#with data}}
    <div class="main__content">
        <h1 class="title title--center">Пошук плейлиста
        </h1>
        <form method="post" action="/spotify-search" class="search">
            <img src="svg/search.svg">
            <input type="search" class="search__input" placeholder="Введіть запит" name="value" value="{{value}}">
            <button class="search__button">Пошук</button>
        </form>

        <ul class="playlist__list">
            {{#each list}}
            <a href="spotify-playlist?id={{id}}">
                <li class="playlist">
                    <img src="{{image}}" alt="{{name}}" height="100px">
                    <div class="playlist__content">
                        <strong class="playlist__name">{{name}}</strong>
                        <span class="playlist__amount">Плейлист - {{amount}} пісень</span>
                    </div>
                </li>
            </a>
            {{/each}}
        </ul>

    </div>
    {{/with}}
</main>